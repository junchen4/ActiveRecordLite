require_relative 'db_connection'
require_relative '01_sql_object'

module Searchable
  # def where(params)
      # self.all.select do |obj|
      #   params.all? do |col, val|
      #     obj.send(col) == val
      #   end
      # end
end

class SQLObject
  # extend Searchable
end
